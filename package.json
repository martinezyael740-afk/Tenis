{
  "name": "matchpoint-analytics",
  "version": "1.0.0",
  "description": "Matchpoint Analytics — analizador de tenis, empaquetado como app Android con Capacitor.",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
